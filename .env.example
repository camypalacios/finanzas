# Variables de entorno — NUNCA subir este archivo a git
# Copiar a .env y completar con valores reales

# ──── Base de Datos MySQL (Hostinger) ────
DB_HOST=srv1661.hstgr.io
DB_PORT=3306
DB_NAME=u330480178_matias
DB_USER=u330480178_matias
DB_PASSWORD=TU_PASSWORD_AQUI

# ──── JWT ────
JWT_SECRET=cambia_esto_por_una_cadena_aleatoria_larga_minimo_64_chars

# ──── App Config ────
APP_USER=matias
# Password hasheada con bcrypt (generar con: node -e "require('bcryptjs').hash('tu_pass',10).then(console.log)")
APP_PASSWORD_HASH=HASH_BCRYPT_AQUI

# ──── OpenAI (para OCR de PDFs) ────
OPENAI_API_KEY=TU_OPENAI_KEY_AQUI

# ──── Frontend (Vite) ────
VITE_API_URL=/api
